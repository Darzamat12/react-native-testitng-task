export interface ICurrentDate {
  month: string;
  day: string;
  hour: number;
  minute: number;
  id: string;
}

export interface IСoords {
  longitude?: string;
  latitude?: string;
  continent?: string;
  countryName?: string;
  principalSubdivision?: string;
  locality?: string;
}
